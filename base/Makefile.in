# $Id$

CC = @CC@
CFLAGS = @CFLAGS@
LDFLAGS = @LDFLAGS@
LIBS = @LIBS@

BUILD = FBBus

.PHONY: all clean distclean $(BUILD)

all: FBBus

FBBus::
	$(MAKE) -C $@

distclean: clean
	rm -f Makefile */Makefile config.h

clean:
	for i in $(BUILD); do \
		$(MAKE) -C $$i clean ; \
	done
	rm -rf ./Binary
